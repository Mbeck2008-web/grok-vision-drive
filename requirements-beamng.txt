# Windows Tech / retail capture extras (optional). Keep requirements-viz.txt light.
-r requirements.txt
beamngpy>=1.26
bettercam>=1.0.0; platform_system=="Windows"
mss>=9.0.0
pywin32>=306; platform_system=="Windows"  # BeamNG window title match
